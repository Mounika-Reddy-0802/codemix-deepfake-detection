# Reproduce

> Placeholder (finalised Week 10, owner M).
> This file will list the exact steps + configs + seeds to regenerate every table
> and figure in the paper from archived checkpoints.

## Planned sections
- [ ] Environment (`requirements.txt`, Python 3.10, CUDA notes)
- [ ] Data preparation (`scripts/01_download_data.sh` -> `scripts/02_preprocess_all.sh`)
- [ ] Stage-1 baseline (`scripts/04_train_baseline.sh` + `configs/train_baseline.yaml`)
- [ ] Gap matrix (`scripts/05_eval_gap_matrix.sh` + `configs/eval_matrix.yaml`)
- [ ] Stage-3 LoRA (`scripts/06_train_lora.sh` + `configs/train_lora_codemix.yaml`)
- [ ] Figures + tables regeneration
